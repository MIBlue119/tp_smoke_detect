global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: smoke-detect
    metrics_path: /metrics
    static_configs:
      - targets: ["smoke-detect:8000"]

rule_files:
  - /etc/prometheus/rules.yml
