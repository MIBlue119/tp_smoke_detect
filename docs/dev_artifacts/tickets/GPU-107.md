# GPU-107 — One-stream, 20-stream, and fault-injection qualification

Status: implemented; target-host gates blocked with an evidence-bearing receipt.

## Delivered

- Added `scripts/qualify_gpu.py`, a fail-closed runner for the RTX 3090 lab
  profile. It probes the host and NVIDIA Docker runtime, attempts the exact
  digest-pinned DeepStream 7.0 Triton image import with a bounded timeout,
  checks container GPU/DeepStream tools, validates the model/engine manifest,
  validates a sealed local replay pack, and only then permits an operator-
  supplied real runtime command.
- Runtime telemetry accepts JSON lines from the reviewed pipeline and records
  scheduled/processed/dropped samples, queue depth/age, candidate latency,
  GPU/NVDEC utilization, and VRAM samples. Missing telemetry prevents a pass.
- Fault-injection requests are recorded explicitly. No timeout, OOM, stalled
  source, mismatch, queue-expiry, malformed-output, or reset result is inferred
  without a runtime-specific command and evidence.
- Added contract tests in `tests/gpu/test_one_stream.py` and
  `tests/load/test_gpu_qualification.py`.
- Added `tests/load/replay-pack.yaml` as a deliberately metadata-only shape;
  it cannot qualify until replaced by an approved sealed local bundle.

## Qualification attempt: 2026-08-24

Receipt: `docs/dev_artifacts/qualification/2026-08-24-gpu-107-pull-attempt.json`
and its Markdown rendering.

- Host: ready — RTX 3090, compute capability 8.6, driver 580.126.09,
  24576 MiB; Docker NVIDIA runtime registered.
- Exact image import: blocked — `docker pull` was attempted for 30 seconds by
  the harness after a preceding 150-second manual attempt. Layers were still
  downloading and no exact digest-pinned image was imported.
- Model preflight: blocked — checked-in manifest is metadata-only; artifact,
  engine, SBOM, license, lineage, calibration, and rollback hashes are
  unresolved.
- Replay preflight: blocked — checked-in pack is synthetic/metadata-only, not
  sealed, has no local source paths, and has no clip hashes.
- Real DeepStream one-stream path: not run because its image, models, and
  sealed replay prerequisites are not satisfied. No fake metrics or pass was
  produced.
- 20-stream/two-hour and fault-injection gates: not run for the same reason.

## Verification

```text
uv run ruff format --check scripts/qualify_gpu.py tests/gpu/test_one_stream.py tests/load/test_gpu_qualification.py  # PASS
uv run ruff check scripts/qualify_gpu.py tests/gpu/test_one_stream.py tests/load/test_gpu_qualification.py        # PASS
uv run mypy scripts/qualify_gpu.py --follow-imports=skip --ignore-missing-imports                                      # PASS
uv run pytest tests/gpu/test_one_stream.py tests/load/test_gpu_qualification.py -q                                  # 5 passed
uv run python scripts/qualify_gpu.py --no-pull --command-timeout 5 ...                                             # blocked, expected
uv run python scripts/qualify_gpu.py --pull --command-timeout 30 ...                                              # blocked, expected
```

The full CPU/native/Docker gates remain owned by the integration caller. This
unit does not claim GPU qualification until the recovery steps in the receipt
are completed on the target host.

Related plan unit: U7. Related requirements: R1, R11-R17; AE1, AE2, AE5, AE6,
AE8.
