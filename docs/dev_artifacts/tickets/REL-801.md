# REL-801 — U12 release hardening and coding-agent handoff

Status: implementation complete; Sol structured review and release-owner gates pending

## Scope delivered

- Replaced the minimal README with a release-aware product boundary, CPU
  quickstart, repository map, mode safety rules, deployment checks, and
  qualification boundary.
- Expanded AGENTS.md into a clean-checkout takeover guide covering ticket
  ownership, GitFlow/worktrees, explicit staging, remote updates, artifact
  discipline, safety boundaries, and Sol review handoff.
- Added installation, operations, incident-response, and model-release runbooks
  with repository-relative commands and fail-closed procedures.
- Added a release checklist/receipt that records local evidence and explicitly
  leaves external GPU, model, site, agency, legal, and image-digest gates open.
- Removed a stale host-specific virtualenv path from the INT-701 CPU receipt.
  The qualification helper now emits the repository-relative uv command for
  future receipts as well.

## Verification

Commands run on the release worktree:

    uv run ruff format --check .
    uv run ruff check .
    uv run mypy src tests
    uv run pytest tests/unit tests/contract tests/integration tests/e2e -q
    uv run smoke-detect schema --output schemas/smoke/v1
    uv run smoke-detect validate-config configs/camera.example.yaml
    uv run smoke-detect demo --fixture synthetic
    docker compose -f deploy/compose.yaml config --quiet
    git diff --check

Results and any baseline failures are recorded in the release receipt
docs/dev_artifacts/qualification/2026-08-24-rel-801-release.md.

The plan-listed commands smoke-detect verify-release and smoke-detect
audit-artifacts are not implemented by the current CLI; they remain explicit
release-process blockers and are not presented as passing checks.

## Unresolved gates

- Sol structured code review and finding resolution.
- REQUIRED-BEFORE-RELEASE image digests, SBOMs, and vulnerability dispositions.
- GPU/DeepStream/Triton, target 20-camera, 24-hour, and fault-isolation proof.
- Sealed site quality/confusion report and model provenance/legal/procurement.
- Approved retention policy, silent-period acceptance, physical audio, and
  agency sign-off.

No automatic-audio, GPU-capacity, legal, or production claim is made by this
ticket.
